# Benchmark-correction configuration

Copy `benchmark_correction.template.yaml` to `benchmark_correction.yaml`, then provide repository-relative data outputs plus external video and credential locations available in your environment. The local configuration is ignored by Git.
