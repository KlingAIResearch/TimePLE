# Training-data inference configurations

Copy one of the `*.template.yaml` files to a local, ignored configuration file before editing it. `infer_models_config.template.yaml` documents Gemini and Qwen together; `qwen_infer.template.yaml` is a smaller vLLM-only example. Keep credentials and machine-specific hostfiles outside version control.
