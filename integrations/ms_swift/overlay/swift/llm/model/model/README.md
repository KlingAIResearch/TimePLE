# model

This directory is part of the TimePLE project. Its path is `integrations/ms_swift/overlay/swift/llm/model/model`; files here use project-relative paths and are documented by the parent directory.
