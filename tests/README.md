# Tests

This directory contains lightweight, CPU-friendly checks for the standalone TimePLE codec, public training and evaluation configurations, and integration-overlay installer. The tests do not download models or start distributed training.
